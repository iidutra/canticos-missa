# Railway / produção
NODE_ENV=production
PORT=3000

# Injetado automaticamente ao vincular PostgreSQL no Railway
# DATABASE_URL=postgresql://user:pass@host:5432/railway

# Opcional: separar dados de múltiplas paróquias no mesmo banco
WORKSPACE_ID=default
